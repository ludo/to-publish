- title "New Page"
- menu :pages

%h2 new page

= error_messages_for :page

= form_for @page, :action => url(:admin_pages) do
  = partial :form
  = submit "Create"
  or
  = link_to "cancel", url(:admin_pages)